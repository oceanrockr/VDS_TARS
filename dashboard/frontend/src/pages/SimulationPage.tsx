import React from 'react';
import { Typography } from '@mui/material';

const SimulationPage: React.FC = () => {
  return <Typography variant="h4">What-If Simulation</Typography>;
};

export default SimulationPage;
