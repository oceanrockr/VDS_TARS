████████╗ █████╗ ██████╗ ███████╗
╚══██╔══╝██╔══██╗██╔══██╗██╔════╝
   ██║   ███████║██████╔╝███████╗
   ██║   ██╔══██║██╔══██╗╚════██║
   ██║   ██║  ██║██║  ██║███████║
   ╚═╝   ╚═╝  ╚═╝╚═╝  ╚═╝╚══════╝

T.A.R.S. (Temporal Augmented Retrieval System) {{ .Chart.AppVersion }}
Successfully deployed to {{ include "tars.namespace" . }} namespace!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 DEPLOYMENT STATUS

  Release Name:    {{ .Release.Name }}
  Namespace:       {{ include "tars.namespace" . }}
  Chart Version:   {{ .Chart.Version }}
  App Version:     {{ .Chart.AppVersion }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 COMPONENTS DEPLOYED

{{- if .Values.backend.enabled }}
  ✓ Backend Service        ({{ .Values.backend.replicaCount }} replicas)
{{- end }}
{{- if .Values.postgresql.enabled }}
  ✓ PostgreSQL Database    (Analytics)
{{- end }}
{{- if .Values.redis.enabled }}
  ✓ Redis Cache            ({{ .Values.redis.config.cacheTTL }}s TTL)
{{- end }}
{{- if .Values.chromadb.enabled }}
  ✓ ChromaDB               (Vector Storage)
{{- end }}
{{- if .Values.ollama.enabled }}
  ✓ Ollama LLM             ({{ .Values.ollama.model.name }})
{{- end }}
{{- if .Values.ingress.enabled }}
  ✓ Ingress Controller     ({{ .Values.ingress.className }})
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🌐 ACCESS INFORMATION

{{- if .Values.ingress.enabled }}

Your T.A.R.S. instance is accessible at:

{{- range .Values.ingress.hosts }}
  • https://{{ .host }}
{{- end }}

{{- if .Values.certManager.enabled }}
TLS certificates are automatically managed by cert-manager.
{{- end }}

API Endpoints:
  - Health Check:    /health
  - Ready Check:     /ready
  - API Docs:        /docs
  - Metrics:         /metrics/prometheus
  - RAG Query:       /rag/query

{{- else }}

To access T.A.R.S., run:

  kubectl port-forward -n {{ include "tars.namespace" . }} \
    svc/{{ include "tars.backend.serviceName" . }} 8000:8000

Then open: http://localhost:8000

{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔒 SECURITY CHECKLIST

{{- if or (contains "REPLACE_WITH" .Values.secrets.jwtSecretKey) (contains "REPLACE_WITH" .Values.secrets.postgresPassword) }}

⚠️  WARNING: Default secrets detected!

Please update the following before production use:

  helm upgrade {{ .Release.Name }} ./charts/tars \
    --set secrets.jwtSecretKey=$(openssl rand -base64 32) \
    --set secrets.postgresPassword=$(openssl rand -base64 16) \
    --set postgresql.auth.password=$(openssl rand -base64 16) \
    --set certManager.email=your-email@domain.com

{{- else }}

✓ Secrets configured
{{- if .Values.config.security.enableHTTPS }}
✓ HTTPS enforcement enabled
{{- end }}
{{- if gt (int .Values.config.security.rateLimitPerMinute) 0 }}
✓ Rate limiting active ({{ .Values.config.security.rateLimitPerMinute }} req/min)
{{- end }}

{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 MONITORING

{{- if .Values.prometheus.enabled }}

Prometheus metrics available at:
  • Scrape endpoint: /metrics/prometheus
  • Port: {{ .Values.config.prometheus.port }}

Check metrics:
  kubectl port-forward -n {{ include "tars.namespace" . }} \
    svc/{{ include "tars.backend.serviceName" . }} 9090:9090

{{- end }}

View logs:
  kubectl logs -f -n {{ include "tars.namespace" . }} \
    deployment/{{ include "tars.fullname" . }}-backend

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🛠️  USEFUL COMMANDS

Check deployment status:
  kubectl get all -n {{ include "tars.namespace" . }}

View pod status:
  kubectl get pods -n {{ include "tars.namespace" . }} -l app.kubernetes.io/name={{ include "tars.name" . }}

Describe backend pods:
  kubectl describe deployment -n {{ include "tars.namespace" . }} {{ include "tars.fullname" . }}-backend

Test RAG query:
  TOKEN=$(curl -s -X POST https://{{ (index .Values.ingress.hosts 0).host }}/auth/authenticate \
    -H "Content-Type: application/json" \
    -d '{"client_id": "test"}' | jq -r '.access_token')

  curl -s -X POST https://{{ (index .Values.ingress.hosts 0).host }}/rag/query \
    -H "Authorization: Bearer $TOKEN" \
    -H "Content-Type: application/json" \
    -d '{"query": "test query", "top_k": 3}'

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 DOCUMENTATION

  GitHub:      https://github.com/oceanrockr/VDS_TARS
  Quick Start: PHASE7_QUICKSTART.md
  Full Report: PHASE7_IMPLEMENTATION_REPORT.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Thank you for using T.A.R.S.! 🚀
