# ==============================================================================
# T.A.R.S. Home Network Environment Configuration
# Version: v1.0.10 (GA) - Phase 21 User Testing
# ==============================================================================
#
# IMPORTANT: Copy this file to tars-home.env and set your values
#   cp tars-home.env.template tars-home.env
#
# NEVER commit tars-home.env to version control!
# ==============================================================================

# ------------------------------------------------------------------------------
# REQUIRED - Must be set before starting
# ------------------------------------------------------------------------------

# PostgreSQL password (generate a strong password)
# Example: openssl rand -base64 32
TARS_POSTGRES_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD

# JWT Secret Key (generate a strong secret)
# Example: openssl rand -hex 64
TARS_JWT_SECRET=CHANGE_ME_GENERATE_STRONG_SECRET

# ------------------------------------------------------------------------------
# DATABASE CONFIGURATION
# ------------------------------------------------------------------------------
TARS_POSTGRES_DB=tars_home
TARS_POSTGRES_USER=tars

# ------------------------------------------------------------------------------
# LLM CONFIGURATION
# ------------------------------------------------------------------------------
# Default model - pull before first use:
#   docker exec tars-home-ollama ollama pull mistral:7b-instruct
OLLAMA_MODEL=mistral:7b-instruct

# Alternative models (uncomment to switch):
# OLLAMA_MODEL=llama3:8b
# OLLAMA_MODEL=codellama:7b-instruct

# ------------------------------------------------------------------------------
# LOGGING
# ------------------------------------------------------------------------------
LOG_LEVEL=INFO
# DEBUG, INFO, WARNING, ERROR

# ------------------------------------------------------------------------------
# NAS CONFIGURATION (if different from defaults)
# ------------------------------------------------------------------------------
# NAS_HOSTNAME=synology-nas.local
# NAS_IP=192.168.1.20
# NAS_SHARE=LLM_docs
# NAS_MOUNT_POINT=/mnt/llm_docs

# ------------------------------------------------------------------------------
# OPTIONAL - Advanced Configuration
# ------------------------------------------------------------------------------

# Service ports (defaults shown)
# BACKEND_PORT=8000
# CHROMADB_PORT=8001
# OLLAMA_PORT=11434
# REDIS_PORT=6379
# POSTGRES_PORT=5432
# FRONTEND_PORT=3000

# Performance tuning
# MAX_CONCURRENT_REQUESTS=20
# REQUEST_TIMEOUT_SECONDS=120

# CORS origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000,http://192.168.1.*:3000
